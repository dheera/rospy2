from setuptools import setup

setup(
    name='rospy2',
    version='1.0.3',
    install_requires=[],
    package_dir = {
        "rospy2": "src/rospy2",
    },
    packages = [
        "rospy2",
    ],
)
